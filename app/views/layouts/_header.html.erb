 <nav class="navbar-expand-lg navbar-light gnav";>
    <div class = "container">

        <div class="row">
          <div class="col-sm-2">
              <div class="logo">
                <%= link_to root_path, class: "navbar-brand" do %>
                  <%= image_tag("/assets/パンダアイコン4.png", size: '40x40') %> Agepan
                <% end %>
              </div>
          </div>
          <div class="col-sm-6", style="margin-top: 5px;">
            <!-- ナビゲーションバー -->
            <%= render 'layouts/link_header' %>
          </div>
          <div class="col-sm-4", style="margin-top: 10px;">
            <div class= "form-genre">
              <%= form_tag(search_path, method: :get) do %>
                <%= select_tag 'range', options_for_select([['--選択--', ''], ['ユーザー', '1'], ['投稿', '2']]), class: "search_genre" %>
                <!--　下の行は前方一致かなど決めるためのものですが、部分一致しかいらないかなと判断しました。search controllerの方で変数に部分一致を入れています。　-->
                <!--<= select_tag 'search', options_for_select([["前方一致","forward_match"], ["後方一致","backward_match"], ["完全一致","perfect_match"], ["部分一致","partial_match"]]), class: 'form-janre' %>-->
                <%= text_field_tag :word, "", {class: "search_text"} %>
                <%= button_tag '<i class="fa fa-search" style="color: green;"></i>'.html_safe, :type => "submit", :class =>"btn btn-default btn-lg" %>
              <% end %>
            </div>
          </div>
        </div>

      </div>
  </nav>